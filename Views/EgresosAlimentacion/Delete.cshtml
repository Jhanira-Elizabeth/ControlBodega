@model ControlBodega.Models.EgresoAlimentacion
<link rel="stylesheet" href="~/css/planta-dashboard.css" />
@{
    ViewData["Title"] = "Eliminar Egreso Alimentación";
}
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-7">
            <div class="card shadow dashboard-card">
                <div class="card-header bg-danger text-white fw-bold">Eliminar Egreso Alimentación</div>
                <div class="card-body">
                    <h5>¿Estás seguro que deseas eliminar este egreso de alimentación?</h5>
                    <dl class="row">
                        <dt class="col-sm-4">Planta</dt>
                        <dd class="col-sm-8">@Model.Planta?.NPlanta</dd>
                        <dt class="col-sm-4">Fecha</dt>
                        <dd class="col-sm-8">@Model.Fecha.ToString("dd/MM/yyyy")</dd>
                        <dt class="col-sm-4">Responsable</dt>
                        <dd class="col-sm-8">@Model.Responsable</dd>
                        <dt class="col-sm-4">Producto</dt>
                        <dd class="col-sm-8">@Model.Producto</dd>
                    </dl>
                    <form asp-action="Delete">
                        <input type="hidden" asp-for="IdEgresoA" />
                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                            <button type="submit" class="btn btn-danger">Eliminar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
